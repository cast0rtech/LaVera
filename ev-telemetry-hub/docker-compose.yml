version: '3.8'

services:
  # 0. Hub Web Offline y API REST
  lavera-hub:
    build:
      context: .
      dockerfile: Dockerfile.all-in-one
    image: lavera-hub:all-in-one
    container_name: lavera_hub
    ports:
      - "8080:8080"
    environment:
      - TZ=Europe/Zurich
      - PORT=8080
      - LAVERA_DB_PATH=/app/data/lavera.db
      - POSTGRES_HOST=timescaledb
      - POSTGRES_PORT=5432
      - POSTGRES_USER=lavera
      - POSTGRES_PASSWORD=LaVeraSecurePass2026!
      - POSTGRES_DB=lavera_telemetry
    volumes:
      - ./data:/app/data
    restart: always
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/api/health', timeout=3)"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - telemetry_net

  # 1. Extractor Multimarca
  homeassistant:
    image: lscr.io/linuxserver/homeassistant:latest
    container_name: telemetry_ha
    ports:
      - "8123:8123"
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Europe/Zurich
    volumes:
      - ./data/homeassistant:/config
    restart: always
    networks:
      - telemetry_net

  # 2. Base de Datos de Series Temporales (TimescaleDB / PostgreSQL 16)
  timescaledb:
    image: timescale/timescaledb:latest-pg16
    container_name: telemetry_timescale
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=lavera
      - POSTGRES_PASSWORD=LaVeraSecurePass2026!
      - POSTGRES_DB=lavera_telemetry
      - TZ=Europe/Zurich
    volumes:
      - timescale_data:/var/lib/postgresql/data
      - ./sql/init-timescaledb.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U lavera -d lavera_telemetry"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: always
    networks:
      - telemetry_net

  # 3. Procesamiento y Logica
  nodered:
    image: nodered/node-red:latest
    container_name: telemetry_nodered
    ports:
      - "1880:1880"
    environment:
      - TZ=Europe/Zurich
    volumes:
      - ./data/nodered:/data
    restart: always
    networks:
      - telemetry_net

  # 4. Dashboard de Visualizacion
  grafana:
    image: grafana/grafana:latest
    container_name: telemetry_grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - TZ=Europe/Zurich
    volumes:
      - ./data/grafana:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
    restart: always
    depends_on:
      timescaledb:
        condition: service_healthy
    networks:
      - telemetry_net

networks:
  telemetry_net:
    driver: bridge

volumes:
  timescale_data:
